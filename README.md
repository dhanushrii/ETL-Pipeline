# ETL-Pipeline
An end-to-end ETL pipeline that ingests distributor data from Email (IMAP) and SFTP in multiple formats (CSV, Excel, TXT), performs schema normalization, data cleaning, validation against business rules and master data, and routes validated records to final tables while generating automated error reports for invalid data.
